# Frontend Environment Configuration
# Copy this file to `.env` and customize for your deployment

# Public domain (shown in curl examples, API URLs, etc.)
VITE_DOMAIN=goexchange.top

# Build for production:
#   VITE_DOMAIN=your-domain.com npm run build

# Build for local development:
#   npm run dev  # uses defaults from web/src/lib/config.ts
